'use strict';

/**
 * Implement getPages function:
 *
 * Given an object, create script which returns array with pages.
 *
 * Examples:
 * getPages({ totalCount: 42, perPage: 10 }) should return array with 5 numbers
 *
 * @param {object} phrase
 *
 * @return {array}
 */
const getPages = ({ totalCount, perPage }) => {
  // write code here
};

module.exports = getPages;
