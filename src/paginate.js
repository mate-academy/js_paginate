'use strict';

/**
 * Implement paginate function:
 *
 * Given an object, create script which returns sliced items array.
 *
 * Examples:
 * getPages({ items: todos, perPage: 5, currentPage: 2 })
 *  should return items from 6 to 10
 *
 * @param {object} phrase
 *
 * @return {array}
 */
const paginate = () => {
  // write code here
};

module.exports = paginate;
